export class Movie {
  constructor(public director_name: string, public duration: number, public genres: string,
    public movie_imdb_link: string, public title_year: number, public imdb_score: number,
      public movie_title: string) {

  }
}
